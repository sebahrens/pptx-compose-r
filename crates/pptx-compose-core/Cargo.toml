[package]
name = "pptx-compose-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
zip.workspace = true

[dev-dependencies]
serde = { version = "=1.0.228", features = ["derive"] }
toml = "=0.9.8"
